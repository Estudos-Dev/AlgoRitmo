spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://switchyard.proxy.rlwy.net:19988/sales_db
    username: sales_manager
    password: RwM#FDrwNk!1ASzJuwTd
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: false
        use_sql_comments: false

  flyway:
    enabled: true
    baseline-on-migrate: true
    connect-retries: 3
    connect-retries-interval: 10s

  docker:
    compose:
      enabled: false

logging:
  level:
    org.hibernate.SQL: INFO
    org.hibernate.type: INFO
    org.springframework.jdbc.core.JdbcTemplate: INFO
    org.springframework.transaction: INFO